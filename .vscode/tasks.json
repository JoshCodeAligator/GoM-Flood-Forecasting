{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Check baseline",
        "type": "shell",
        "command": "${config:python.defaultInterpreterPath} -m hydro_pipeline.cli --task check-baseline",
        "group": "build",
        "problemMatcher": [],
        "options": {
          "env": {
            "PYTHONPATH": "${workspaceFolder}/src"
          }
        }
      }
    ]
  }
  